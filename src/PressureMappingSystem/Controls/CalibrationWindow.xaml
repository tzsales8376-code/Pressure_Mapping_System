<Window x:Class="PressureMappingSystem.Controls.CalibrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="自訂校正 - Custom Calibration" Width="700" Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF1E1E2E">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF2D2D3D"/>
            <Setter Property="Foreground" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#44FFFFFF"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="CalBtn" TargetType="Button">
            <Setter Property="Background" Value="#FF0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CaptureBtn" TargetType="Button" BasedOn="{StaticResource CalBtn}">
            <Setter Property="Background" Value="#FF2D7D3A"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Width" Value="60"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="校正流程：放置已知砝碼 → 點擊「擷取」讀取感測器數值 → 點擊「計算校正」產生校正表"
                   FontSize="12" Foreground="#FF888888" Margin="0,0,0,12" TextWrapping="Wrap"/>

        <!-- Table Header -->
        <Grid Grid.Row="1" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="砝碼重量" FontWeight="Bold" Foreground="#FF0078D4"/>
            <TextBlock Grid.Column="1" Text="" />
            <TextBlock Grid.Column="2" Text="加權前數值 (Raw)" FontWeight="Bold" Foreground="#FFFFAA44" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="3" Text="加權後數值 (Cal)" FontWeight="Bold" Foreground="#FF44DD88" HorizontalAlignment="Center"/>
        </Grid>

        <!-- Calibration Rows -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="CalRows"/>
        </ScrollViewer>

        <!-- Status -->
        <TextBlock Grid.Row="3" x:Name="StatusLabel" Text="請先連接感測器並啟用歸零 (Zero)"
                   Foreground="#FF888888" FontSize="12" Margin="0,8,0,8"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="清除全部" Style="{StaticResource CalBtn}" Background="#FF666666" Click="ClearAll_Click"/>
            <Button Content="計算校正" Style="{StaticResource CalBtn}" Background="#FFFFAA44" Foreground="#FF1E1E2E"
                    FontWeight="Bold" Click="Calculate_Click"/>
            <Button Content="套用校正" Style="{StaticResource CalBtn}" Background="#FF44DD88" Foreground="#FF1E1E2E"
                    FontWeight="Bold" Click="Apply_Click"/>
            <Button Content="💾 儲存設定" Style="{StaticResource CalBtn}" Background="#FF0078D4" FontWeight="Bold" Click="Save_Click"/>
            <Button Content="關閉" Style="{StaticResource CalBtn}" Background="#FF444444" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
